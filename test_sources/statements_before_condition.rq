[function [main], ()[signed_integer],
    [result] :\[var]= 0;
    [if
        var0 := 0;
        var1 := 1;
        // the following line ends in comma, so its the condition of this if scope.
        var0 == var1, // this fails, so the next line never executes.
        [result] = 1;
    ];
];