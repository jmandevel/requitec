[import "std"];

[function myFunc, (>a:fast\Int32, b:fast\Int32<)fast\Int32,
    [if a > b,
        std.print(line="a is greater than b");
    ][else_if b > a,
        std.print(line="b is greater than a");
    ][else 
        std.print(line="a and b are equal");
    ];
    [result] <:= a + b;
];

[main ()si,
    std.print(line="the return value is " +> myFunc(1, 2));

    [result] <:= 0;
];