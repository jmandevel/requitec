[import "std"];

[static][anchor]::Loop
[for 0.<=10, num:=[value],
    [static]
    varname :\[var]= ..std.format(text="VAR" +> num);
    [outside_scope]::Loop[global]
    varname.[identify] :\[var]= index;
];

[entry
    [static]
    [for 0.<=10, num:=[value],
        [static]
        varname :\[var]= ..std.format(text="VAR" +> num);
        ..std.print(line=varname.[borrow] +> " is " +> ..varname.[identify] +> "!");  
    ];

    [result] := 0;
];