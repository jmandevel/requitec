[import "std"];

@static
[for 0.<=10, num:=[value],
    @global @capture..{num:=num}
    \std.format(text="VAR{num}") :`= index;
];

[entry
    @static
    [for 0.<=10, num:=[value], 
        @static
        varname := std.format(text="VAR{num}");
        std.print(line="{varname.[borrow].span()} is {\varname}!");        
    ];
];