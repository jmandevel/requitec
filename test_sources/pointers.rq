[entry
    // if you know C, this should be super easy to understand.

    var :`[single]= 0;

    var_ptr :`*`[single]= var.[address];

    var_ptr.[content] = 1;

    var_copy := var_ptr.[content];

    var_ptr = [null];

    [result] := var_copy.[float_cast];
];