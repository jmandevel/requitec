[class MyObject, 
    static var := 0;
    static el := Int;
    {>public arr : var#el<}, 
];

compile_time VAR := 0;

[main 
    static Return := si;
    ()Return,

    [result] :var\= 0; 

    [if var0 == var1,
        [result] = 1;
    ][else_if false, 
    
    ][else
    
    ];
];

opaque mangle::puts
[function noname, (>str:?no_var\Char<)Void];
