[class MyObject, 
    [static] var := 0;
    [static] el := [signed_integer];
    {>[public] arr : var#el<}, 
];

[constant] VAR := 0;

[main 
    [static] Return := [signed_integer];
    ()Return,

    [result] :[var]\= 0; 

    [if var0 == var1,
        [result] = 1;
    ][else_if [false], 
    
    ][else
    
    ];
];

[opaque][mangle]::puts
[function [noname], (>str:?[no_var]\[char]<) [void]];
